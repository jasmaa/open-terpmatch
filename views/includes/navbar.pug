nav.navbar.navbar-light.bg-light
  a.navbar-brand Terpmatch
  ul.navbar-nav.mr-auto

  if user
    .dropdown
      button.btn.btn-secondary.dropdown-toggle(data-toggle="dropdown") #{user.uid}
      .dropdown-menu.dropdown-menu-right
        a.dropdown-item(href="#") Profile
        a.dropdown-item(href="/logout") Logout
  else
    a.btn.btn-primary(href="/umd/login") Login